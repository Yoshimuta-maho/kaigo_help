<%= form_with model: genre, url: admin_genres_path, local: true do |form| %>

  <div class="form-group">
    <%= form.label :genre_name, 'ジャンル名' %>
    <%= form.text_field :genre_name, class: 'form-control' %>
  </div>

  <%= form.submit '作成', class: 'btn btn-primary' %>
<% end %>
